[package]
name = "demo-server"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
actix-web = "4"
actix-files = "0.7"

[dev-dependencies]
# Used only by panels.rs's tests, to assemble into a throwaway directory instead of the real demo/index.html.
tempfile = "3"
